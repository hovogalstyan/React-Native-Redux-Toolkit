export const UserData = (state) => state.UserSlice.user;
export const newProducts = (state) => state.NewProductsSlice.data;
export const productsData = (state) => state.ProductSlice;

export const descriptionItem = (state) => state.ProductSlice.item;
export const cartData = (state) => state.CartSlice;
